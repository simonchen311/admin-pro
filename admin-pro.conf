server {
    listen       80;
    server_name  120.27.236.15;
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
    # 转发给服务端接口的规则配置
    # location /api {
    #   proxy_pass http://120.27.236.15:3000;
    # }
}
